# 🗺️ Maps & Location Subsystem

## Map Technologies
- **Flutter Client:** `flutter_map` with OpenStreetMap / Mapbox / Google Maps tile provider.
- **Coordinates:** `latlong2` for spatial calculations.
- **Key Safety:** Maps API keys are passed through `.env` and never checked into version control.
